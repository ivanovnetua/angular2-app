# Angular2 App (education)

Star and compile project with commands:
```sh
$ npm install 
$ npm run tsc
$ npm run start
```
  - npm run tsc; // for compile to .js
  - npm run start; // compile, watch, start lite server